# 🚀 Быстрый старт для разработчика

## Задача
Запустить панель, протестировать все модули, исправить найденные ошибки.

## Шаги

### 1. Установка
```bash
pnpm install
pnpm db:push
pnpm tsc --noEmit  # исправить все ошибки в проектных файлах
```

### 2. Настройка .env
Создать `.env` в корне:
```env
NODE_ENV=development
PORT=3000
DATABASE_URL=mysql://user:pass@host:3306/db_name
JWT_SECRET=случайная_строка_32+_символа
COOKIE_DOMAIN=localhost
OPENAI_API_KEY=sk-...  # ⚠️ НЕ коммитить!
OPENAI_MODEL=gpt-4o-mini
TELEGRAM_BOOKING_BOT_TOKEN=...  # опционально
BOOKING_TENANT_ID=1
```

### 3. Запуск
```bash
pnpm dev
```

### 4. Тестирование
Пройти по чек-листу из `SETUP_AND_TESTING_TZ.md`:
- ✅ Авторизация
- ✅ Базовые сущности (services, masters, clients, calendar)
- ✅ Reports
- ✅ BookingDemo
- ✅ Notifications (настройки, шаблоны, AI, A/B, test-send)
- ✅ Notification Logs (логи, аналитика, конверсия, ИИ-советчик)
- ✅ Business Assistant

### 5. Обработка ошибок
- TypeScript: исправить все ошибки в `server/**`, `client/**`, `drizzle/**`
- Runtime: никаких белых экранов, все ошибки должны быть обработаны
- Интеграции: при отсутствии ключей — понятные сообщения, не падения

### 6. Acceptance Criteria
- ✅ `pnpm tsc --noEmit` без ошибок в проектных файлах
- ✅ `pnpm dev` стартует без фатальных ошибок
- ✅ Все маршруты открываются без падений
- ✅ Все модули работают или показывают понятные ошибки

## ⚠️ Безопасность
**НИКОГДА не коммитить API-ключи!** Использовать `.env` (должен быть в `.gitignore`).

## 📋 Полное ТЗ
См. `SETUP_AND_TESTING_TZ.md` для детального чек-листа и требований.

