# 🔧 Настройка .env файла

## Быстрая настройка

Создай файл `.env` в корне проекта со следующим содержимым:

```env
# NODE / APP
NODE_ENV=development
PORT=3000

# БД (ОБЯЗАТЕЛЬНО - замени на свои данные!)
DATABASE_URL=mysql://root:password@localhost:3306/booking_saas

# AUTH / SECURITY (ОБЯЗАТЕЛЬНО!)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-123456789

# LLM (опционально, для AI функций)
OPENAI_API_KEY=sk-e83db348d60d4b75b262f27d9b0fe736
OPENAI_MODEL=gpt-4o-mini

# TELEGRAM (опционально)
TELEGRAM_BOOKING_BOT_TOKEN=
BOOKING_TENANT_ID=1
TELEGRAM_BOOKING_BOT_USERNAME=
```

## Важно!

1. **DATABASE_URL** - замени `root:password@localhost:3306/booking_saas` на свои данные БД
2. **JWT_SECRET** - замени на случайную строку минимум 32 символа (для production)
3. После создания `.env` перезапусти сервер: `pnpm dev`

## Если .env не создан

В dev режиме система использует дефолтные значения, но это только для тестирования!
Для production обязательно создай `.env` с правильными значениями.

