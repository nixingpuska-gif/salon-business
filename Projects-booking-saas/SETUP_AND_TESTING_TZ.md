# ТЗ: Запуск панели и фикс ошибок

## 🎯 Цель итерации

Сделать так, чтобы:
1. Веб-панель (admin + публичные части) **стабильно запускалась** в dev и на сервере.
2. Все новые модули (отчёты, уведомления, Telegram, AI-шаблоны, ассистент и т.д.) были **протестированы вручную по чек-листу**.
3. Все ошибки (TypeScript, runtime, tRPC, интеграции) были:
   - найдены,
   - классифицированы,
   - либо исправлены, либо оформлены как известные ограничения с понятными сообщениями.

---

## 📦 Контекст проекта

Full-stack приложение:
- **Backend**: Node.js + tRPC v11 + Drizzle ORM + MySQL
- **Frontend**: React + TypeScript + Tailwind (luxury theme)
- **Доп. модули**:
  - Auth + multi-tenant (`tenantId`)
  - Reports (analytics, Recharts)
  - Notification Service + worker (напоминания)
  - Telegram Booking Bot (+ /booking, /my, cancel, reminders)
  - Notification Templates, A/B-тесты, AI-генерация текста, test-send
  - Business Assistant (ИИ-ассистент владельца)
  - Client Booking (publicBooking API + BookingDemo)
- **LLM-провайдер**: через SDK (`openai`-совместимый, сейчас DeepSeek key → мапим в ENV)

---

## 🔧 Требования к окружению

### Софт
- Node.js: **LTS (>=18)**
- pnpm: установлен глобально
- MySQL / MariaDB: живой инстанс БД
- Git, bash/PowerShell

### ENV-переменные (минимальный набор)

В `.env` (в корне проекта):

```env
# NODE / APP
NODE_ENV=development
PORT=3000

# БД
DATABASE_URL=mysql://user:pass@host:3306/db_name

# AUTH / SECURITY
JWT_SECRET=случайная_строка_минимум_32_символа
COOKIE_DOMAIN=localhost

# LLM (сюда кладём ключ DeepSeek или прокси)
OPENAI_API_KEY=sk-...  # ⚠️ НИКОГДА не коммитить в репозиторий!
OPENAI_MODEL=gpt-4o-mini  # или модель из твоего провайдера

# TELEGRAM BOOKING BOT (опционально)
TELEGRAM_BOOKING_BOT_TOKEN=...  # опционально, для теста бота
BOOKING_TENANT_ID=1             # id тестового тенанта
TELEGRAM_BOOKING_BOT_USERNAME=your_bot_username
```

> ⚠️ **Важно**: если какого-то токена нет (OpenAI/Telegram) — код должен **не падать**, а аккуратно возвращать понятные ошибки вроде "ИИ не настроен".

---

## 📋 Задачи для разработчика

### 1. Подготовка и запуск

1. **Установить зависимости:**
   ```bash
   pnpm install
   ```

2. **Применить миграции / синхронизировать схему:**
   ```bash
   pnpm db:push
   ```

3. **Проверить типы:**
   ```bash
   pnpm tsc --noEmit
   ```
   - **Важно**: игнорировать ошибки внутри `node_modules`
   - Все ошибки **в наших файлах** (`server/**`, `client/**`, `drizzle/schema.ts` и т.д.) должны быть устранены

4. **Запуск dev-сервера:**
   ```bash
   pnpm dev
   ```

5. **Зафиксировать порт**, на котором крутится фронт (обычно 3000/5173)

---

## ✅ Чек-лист ручного теста панели

### 1. Авторизация и базовый UI
- [ ] Перейти на `/login`
- [ ] Ввод email → создать/использовать тестовый owner
- [ ] После логина → редирект на `/dashboard`
- [ ] Проверить: Sidebar, header, профиль пользователя в правом верхнем углу
- [ ] Logout → редирект на `/login`, защищённость `/dashboard` после выхода

### 2. Базовые сущности
- [ ] `/services` - создать пару услуг, проверить таблицу, редактирование/удаление
- [ ] `/masters` - создать несколько мастеров
- [ ] `/clients` - создать клиентов (можно через записи/бота)
- [ ] `/calendar` - создать запись через админку, проверить видимость в календаре

### 3. Отчёты `/reports`
- [ ] Проверить вкладки: Обзор, Выручка, Мастера, Услуги, Клиенты
- [ ] Фильтры 7/30/90 дней - нет ошибок в консоли
- [ ] Если данных мало — аккуратные заглушки, а не падения

### 4. BookingDemo `/booking-demo`
- [ ] Пройти все 3 шага:
  1. Выбор услуги
  2. Выбор мастера
  3. Выбор даты/слота + ввод имени/телефона
- [ ] После подтверждения:
  - Появляется экран успеха
  - Запись появляется в календаре
  - Конфликт слотов корректно обрабатывается (ошибка, предложение выбрать другой)

### 5. Notifications `/notifications`

#### 5.1. Общие настройки уведомлений
- [ ] Переключатели включения/выключения напоминаний
- [ ] Сохраняются, после перезагрузки страницы значения те же

#### 5.2. Telegram-настройки
- [ ] Поле `Telegram chat ID для тестовых отправок`
- [ ] Ввести тестовый chat id → сохранить

#### 5.3. Шаблоны
- [ ] Выбор канала: `Telegram-бот`
- [ ] Выбор типа: `Напоминание за 24 часа` / `Напоминание за 1 час`
- [ ] Редактирование `title` + `body`
- [ ] Предпросмотр (demo-значения плейсхолдеров)
- [ ] Кнопка **«Сохранить шаблон»** → без ошибок

#### 5.4. AI-генерация
- [ ] Нажать **«Сгенерировать через ИИ»**:
  - При наличии `OPENAI_API_KEY` → появляются варианты
  - Каждый вариант содержит плейсхолдеры `{date}` и `{time}`
- [ ] Нажать **«Использовать этот вариант»**:
  - Текст попадает в форму
  - Можно отредактировать и сохранить

#### 5.5. AI-настройки
- [ ] Секция: тональность (`luxury/friendly/neutral`) + описание бизнеса
- [ ] Изменить тональность и описание → сгенерировать шаблон ещё раз
- [ ] Проверить, что тон реально меняется

#### 5.6. A/B варианты
- [ ] Селектор варианта (A/B) работает
- [ ] Создание варианта B на основе A работает
- [ ] Индикатор "A/B тест активен" отображается при наличии нескольких вариантов

#### 5.7. Test-send
- [ ] Нажать **«Отправить тест»**:
  - В Telegram приходит сообщение `[TEST] ...`
  - В логах создаётся запись с `isTest = true`

### 6. Notification Logs `/notification-logs`

#### 6.1. Вкладка "Логи"
- [ ] Должен показываться список отправок
- [ ] Фильтры по: периоду (7/30/90 дней), каналу, статусу
- [ ] Test-сообщения видны, статус `sent`/`failed` отображён

#### 6.2. Вкладка "Аналитика"
- [ ] Карточки по вариантам A/B (если есть)
- [ ] График Recharts:
  - Без ошибок в консоли
  - Работает переключение периода/фильтров

#### 6.3. Вкладка "Конверсия"
- [ ] Если есть реальные записи с разными статусами (`completed/cancelled/no_show`):
  - Показать таблицу конверсии по вариантам
- [ ] Если данных недостаточно:
  - Внятная заглушка, никаких NaN/ошибок

#### 6.4. Notification Advisor (ИИ)
- [ ] На вкладке "Конверсия" → кнопка **«Спросить ИИ о шаблонах»**:
  - При настроенном `OPENAI_API_KEY`:
    - Спиннер → появляется ответ ИИ с выводами и следующими шагами
  - При отсутствии ключа:
    - Понятное сообщение об ошибке ("ИИ для шаблонов не настроен")

### 7. Business Assistant (ИИ-ассистент владельца)
- [ ] Через ChatDock:
  - Открыть вкладку ассистента для предпринимателя
  - Задать вопросы:
    - "Какая у меня выручка за последние 30 дней?"
    - "Какие услуги приносят больше всего денег?"
- [ ] Проверить:
  - Ответ в формате: краткий вывод → цифры → конкретные действия
  - Если нет данных — он честно пишет, что данных мало, а не выдумывает

### 8. Telegram Booking Bot (опционально)
> Можно сделать отдельным этапом, если сейчас главное — веб-панель

- [ ] Команды: `/start`, `/booking`, `/my`, `/cancel`
- [ ] Флоу:
  - Пройти запись через бота → запись в календаре
  - `/my` показывает записи
  - Кнопка "Отменить" → статус `cancelled`, слот освобождён
- [ ] Напоминания:
  - Для записей через бота в нужное время приходят напоминания (если worker запускается)

---

## 🐛 Работа с ошибками — требования

### TypeScript
**Цель:** `pnpm tsc --noEmit` не должен выдавать ошибок в **нашем** коде.

- Ошибки в `node_modules` игнорируются (если не ломают компиляцию)
- Все ошибки в `server/**`, `client/**`, `drizzle/**` обязаны быть исправлены
- Если тип "слишком сложный" → допускается локальный `// @ts-expect-error` с комментом **почему**

### Runtime-ошибки
**Требование:** при использовании UI ни одна страница не должна:
- падать с белым экраном
- кидать uncaught exception в консоль

Для каждой найденной ошибки:
1. Зафиксировать:
   - шаги воспроизведения
   - трейс из консоли (frontend/backend)
   - предположительную причину
2. Исправить или:
   - временно добавить аккуратную заглушку UI
   - вернуть контролируемую ошибку в tRPC с понятным сообщением

### Интеграции (LLM / Telegram / др.)
Поведение по умолчанию:
- Нет `OPENAI_API_KEY` → ИИ-фичи:
  - не падают
  - возвращают понятный текст "ИИ пока не настроен"
- Нет `TELEGRAM_BOOKING_BOT_TOKEN` или `BOOKING_TENANT_ID`:
  - бот не инициализируется, в логах понятное сообщение
  - панель работает, разделы уведомлений не ломаются
- Любые внешние ошибки (сетевые, 4xx/5xx от API) → логируются с префиксом, но не валят весь процесс

---

## ✅ Acceptance Criteria (что считаем успешным завершением задачи)

1. `pnpm tsc --noEmit` — **без ошибок в проектных файлах**
2. `pnpm dev` — сервер стартует без фатальных ошибок
3. Все маршруты:
   - `/login`, `/dashboard`, `/services`, `/masters`, `/clients`
   - `/calendar`, `/reports`, `/notifications`
   - `/notification-logs`, `/booking-demo`
   — открываются без падений
4. Notification Templates:
   - редактирование, сохранение, AI-генерация, тест-отправка — работают
5. Notification Logs:
   - Логи, аналитика, конверсия — отображаются корректно
   - ИИ-советчик даёт ответ или корректно сообщает о невозможности
6. Business Assistant:
   - Отвечает в нужной структуре
   - Не падает при отсутствии данных/ключей
7. Все найденные ошибки:
   - либо исправлены
   - либо задокументированы с пометкой TODO и описанием последствий

---

## 📝 Отчёт о найденных проблемах

Для каждой найденной ошибки зафиксировать:
1. **Шаги воспроизведения** (пошагово)
2. **Трейс из консоли** (frontend/backend)
3. **Предположительная причина**
4. **Статус**: исправлено / TODO с описанием

---

## 🔒 Безопасность

⚠️ **КРИТИЧЕСКИ ВАЖНО:**
- **НИКОГДА** не коммитить API-ключи в репозиторий
- Использовать `.env` файл (который в `.gitignore`)
- Если ключ был скомпрометирован — немедленно сгенерировать новый и отключить старый
- В коде не должно быть хардкода ключей

---

## 📚 Дополнительные ресурсы

- Схема БД: `drizzle/schema.ts`
- Конфигурация ENV: `server/_core/env.ts`
- tRPC роутеры: `server/routers/`
- Frontend страницы: `client/src/pages/admin/`

