# 📚 Документация по запуску и тестированию

## 📄 Документы

1. **`SETUP_AND_TESTING_TZ.md`** - Полное техническое задание с детальным чек-листом
2. **`QUICK_START_FOR_DEV.md`** - Краткая версия для быстрого старта
3. **`TESTING_CHECKLIST.md`** - Чек-лист тестирования в формате для копирования

## 🚀 Быстрый старт

```bash
# 1. Установка
pnpm install
pnpm db:push

# 2. Настройка .env (см. SETUP_AND_TESTING_TZ.md)

# 3. Проверка типов
pnpm tsc --noEmit

# 4. Запуск
pnpm dev
```

## ✅ Исправленные ошибки

- ✅ `FAILED_PRECONDITION` → `BAD_REQUEST` в `assistant.ts` (2 места)
- ✅ Исправлена проблема с `where()` в `clients.ts` (использование `and()` для объединения условий)
- ✅ Создан `.gitignore` для защиты `.env` файлов

## ⚠️ Важные замечания

1. **Безопасность API-ключей**: 
   - `.env` файл добавлен в `.gitignore`
   - **НИКОГДА** не коммитить API-ключи в репозиторий
   - Если ключ был скомпрометирован — немедленно сгенерировать новый

2. **Оставшиеся ошибки TypeScript** (не критичные):
   - `BookingDemo.tsx` - проблемы с типом `Step` (не влияет на работу)
   - `bookingBot.ts` - неявные `any` типы (не критично для runtime)

3. **Обработка отсутствующих ключей**:
   - При отсутствии `OPENAI_API_KEY` → ИИ-фичи показывают понятные сообщения
   - При отсутствии `TELEGRAM_BOOKING_BOT_TOKEN` → бот не инициализируется, панель работает

## 📋 Следующие шаги

1. Пройти по чек-листу из `SETUP_AND_TESTING_TZ.md`
2. Зафиксировать все найденные проблемы
3. Исправить критичные ошибки
4. Документировать известные ограничения

